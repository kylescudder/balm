[build]
  base = "Site"
  command = "bun run build"
  publish = "dist"
  # Skip deploys unless the site or its Netlify configuration changed.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ../netlify.toml"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
