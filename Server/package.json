{
  "name": "jinear-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jinear BFF — token exchange + refresh for native Atlassian OAuth (PKCE).",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
